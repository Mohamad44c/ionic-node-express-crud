import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-node-express-crud',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
